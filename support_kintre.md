# Kinote サポート

最終更新日: 2026年9月6日

筋トレ記録アプリ「Kinote（キノート）」のサポートページです。ご不明点やお困りのことがあれば、まず下記のよくある質問をご確認ください。解決しない場合は、ページ末尾の連絡先までお問い合わせください。

## アプリについて

| 項目 | 内容 |
|---|---|
| アプリ名 | Kinote（キノート） |
| 対応 OS | iOS 15.0 以降（iPhone 専用） |
| 価格 | 無料（アプリ内課金・広告なし） |
| アカウント | 不要 |
| データの保存先 | 端末内のみ（サーバーへの送信なし） |
| 対応言語 | 日本語 |

## よくある質問

### 記録・入力

**Q. 記録したデータはどこに保存されますか？**
すべて端末内に保存されます。開発者や第三者のサーバーに送信されることはなく、オフラインでもすべての機能を利用できます。

**Q. 保存ボタンが見つかりません。**
Kinote は入力内容を自動保存するため、保存ボタンはありません。セットを入力した時点で記録は保存されています。

**Q. 前回と同じ内容を素早く記録したい。**
セット入力画面の「前回コピー」で、前回実施したセット内容をそのまま呼び出せます。直前セットのコピーにも対応しています。また、「胸の日」「脚の日」などをルーティンとして保存しておくと、1タップで一日分の種目を展開できます。

**Q. 一覧にない種目を記録したい。**
種目マスタ管理から、種目・カテゴリ・バリエーションを自由に追加・編集できます。急いでいるときは「その他」として記録しておき、あとから正式な種目に昇格させることもできます。

**Q. 重量の単位を lb に変更できますか？**
設定 → 一般 → 単位 から kg / lb を切り替えられます。内部では kg で保存しているため、切り替えても過去の記録は正しく換算されて表示されます。

**Q. 負荷係数とは何ですか？**
アシスト付きや軽めの実施を補正するための係数（0.10〜2.00）です。通常の実施では 1.00 のままで問題ありません。ボリュームや推定 1RM の計算に反映されます。

### 統計・グラフ

**Q. 推定 1RM はどのように計算していますか？**
設定 → 統計 → 1RM 計算式 で選択した計算式（Epley / Brzycki など）を使い、重量と回数から推定しています。ウォームアップセットを計算から除外する設定もあります。

**Q. 自己ベスト（PR）の判定基準は？**
種目ごとに、過去の最大重量・推定 1RM を上回った時点で自動判定し、お祝い表示を行います。

### タイマー・通知

**Q. インターバルタイマーの通知が届きません。**
以下をご確認ください。

1. iOS の 設定 → 通知 → Kinote で「通知を許可」がオンになっているか
2. 集中モード（おやすみモードなど）が有効になっていないか
3. アプリを完全に終了（スワイプで終了）していないか

上記を確認しても改善しない場合は、アプリを再起動してからタイマーを再度開始してみてください。

**Q. トレーニングリマインダーを設定したい。**
設定 → リマインダー から、曜日・時刻・メッセージを指定して追加できます。通知の許可が必要です。

**Q. セットを追加したときにタイマーを自動で開始したい。**
設定 → 入力 → タイマー自動開始 をオンにし、既定インターバルを設定してください。

### データのバックアップ・機種変更

**Q. 機種変更するときにデータを引き継げますか？**
はい。旧端末で 設定 → データ → エクスポート/バックアップ から JSON バックアップを作成し、ファイル App や AirDrop などで新端末に転送してください。新端末の Kinote で同じ画面から「復元」を選び、そのファイルを読み込むとすべてのデータが復元されます。

**Q. iCloud で自動バックアップされますか？**
現在のバージョンでは、アプリ独自のクラウド同期は行っていません。iOS の iCloud バックアップ（端末全体のバックアップ）の対象には含まれますが、確実に引き継ぐには上記の JSON バックアップをおすすめします。

**Q. 記録を Excel などで分析したい。**
設定 → データ → エクスポート/バックアップ から、期間を指定して CSV を書き出せます。UTF-8（BOM 付き）で出力されるため、Excel で開いても文字化けしません。種目マスタも CSV で出力できます。

**Q. 他のアプリから記録を移行できますか？**
現在のバージョンでは CSV インポートには対応していません。今後のアップデートで対応を検討しています。

**Q. データをすべて削除したい。**
設定 → データ → データ全削除 から削除できます。この操作は取り消せません。また、アプリをアンインストールすると端末内のすべてのデータが削除されます。開発者側にはデータが一切保存されていないため、開発者への削除依頼は不要です。

### 写真・カメラ

**Q. カメラや写真へのアクセスを求められるのはなぜですか？**
身体写真でビフォーアフターを記録する機能のためです。撮影・選択した画像は端末内にのみ保存されます。この機能を使わない場合、アクセス許可は不要です。

**Q. 写真へのアクセスを誤って拒否してしまいました。**
iOS の 設定 → Kinote から、カメラ・写真のアクセス許可を変更できます。

### 表示・設定

**Q. ダークモードやテーマカラーを変更したい。**
設定 → 一般 から、テーマ（システム / ライト / ダーク）と 8 色のテーマカラー（オレンジ・レッド・ブルー・インディゴ・ティール・グリーン・パープル・ピンク）を選べます。

**Q. 週の開始曜日を変更したい。**
設定 → 一般 → 週の開始曜日 から変更できます。カレンダーや週間サマリーの表示に反映されます。

**Q. iPad で使えますか？**
現在のバージョンは iPhone 専用です。

## 不具合の報告・機能のご要望

不具合を見つけた場合や、追加してほしい機能がある場合は、下記の連絡先までお知らせください。不具合報告の際は、次の情報を添えていただくと調査がスムーズです。

- ご利用の iPhone の機種と iOS バージョン
- Kinote のバージョン（設定 → アプリ情報 で確認できます）
- 発生した操作の手順と、実際に起きたこと
- 可能であればスクリーンショット

## プライバシーポリシー

利用者情報の取り扱いについては、[プライバシーポリシー](./index.html) をご覧ください。

## お問い合わせ

- メール: clear20171009@gmail.com

通常 2〜3 営業日以内に返信いたします。内容によってはお時間をいただく場合があります。
